<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ajudamei.allan_arthur.com.ajuda_mei">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <service
            android:enabled="true"
            android:name=".AtualizarMatPrimaService">
        </service>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TipoEstoqueActivity" />
        <activity android:name=".CaixaDaEmpresaActivity" />
        <activity android:name=".ListaPessoalActivity" />
        <activity android:name=".EmpregadosActivity" />
        <activity android:name=".EscolherMatPrimaActivity" />
        <activity android:name=".AdicionarMatPrimaActivity"></activity>
    </application>

</manifest>